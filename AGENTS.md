# AGENTS.md

このリポジトリを引き継ぐエージェント向けの規約。人間向けの概要は `README.md`。

## このプロジェクトは何か

Attention の O(n²) 計算量問題と KV キャッシュ問題について、2018–2026 年の研究系譜を
調査し、**レビュー論文**と**インタラクティブな関係マップ**にまとめたもの。
成果物は claude.ai の Artifact として公開済み（URL は `README.md`）。

調査基準日は **2026-09-04**。この日以降の文献は入っていない。

## ファイルの役割 — どれが正本か

| パス | 種別 | 備考 |
|---|---|---|
| `map.html` | **正本** | 論文データ（`const D`, `META`, `SUM`）と描画コードを内包する単一ファイル |
| `review.html` | **正本** | レビュー論文本体。全 10 章 |
| `data/papers.json` | 生成物 | `map.html` から書き出した構造化データ。**手で編集しない** |
| `data/papers.csv` | 生成物 | 同上 |
| `PAPERS.md` | 生成物 | 同上（人が読む一覧） |
| `tools/export_papers.py` | 正本 | 上記 3 つを生成するスクリプト |
| `notes/` | 参考 | 作業メモ。成果物ではない |

**`map.html` が論文データの唯一の情報源である。** 生成物を編集して上流へ戻す経路は
存在しない。`map.html` を変えたら必ず再生成する:

```bash
python3 tools/export_papers.py
```

このスクリプトは整合性検査も兼ねており、id 重複・存在しない親 id・`META` / `SUM` の
欠落／余剰があれば異常終了する（終了コード 1）。CI 代わりに使える。

## 開発の進め方

`main` へ直接コミットせず、ブランチを切って PR 経由で入れる。

```bash
git switch -c docs/some-change
# 編集
python3 tools/export_papers.py     # map.html を触ったら必ず再生成（検証を兼ねる）
git add -A && git commit
git push -u origin docs/some-change
gh pr create --base main
```

**OneDrive 配下で作業する場合の注意。** 同期がファイルのパーミッションを書き換えるため、
内容が同一でも実行ビットの差分（`old mode 100644` / `new mode 100755`）が出続け、
`git pull` がそれで停止する。このリポジトリでは `core.fileMode false` を設定して無視している。
実行ビットを意図的に管理したくなった場合だけ、この設定を外すこと。

```bash
git config core.fileMode false
```

## データモデル

`map.html` 内の 3 つの構造。**同じ id で三つすべてに入れる。**

```js
// D: [id, 表示名, 'YYYY-MM', レーン, 会議/出典, arXiv ID, 概要, 限界・含意, [親ノードの id]]
['kimilinear','Kimi Linear (KDA)','2025-10','ssm','arXiv (Moonshot)','2510.26692',
 'Gated DeltaNet のスカラー減衰をチャネル毎の対角ゲートへ細粒度化',
 '3:1 KDA:MLA。KV −75% / decode 6×',['gateddelta']],

// META: id: [論文名（原題）, 著者]
kimilinear:['Kimi Linear: An Expressive, Efficient Attention Architecture','Moonshot AI (Kimi Team)'],

// SUM: id: 要約（150 字程度）
kimilinear:'Gated DeltaNet の大域的な忘却ゲートを、単一のスカラーからチャネルごとの対角行列へ細粒度化する。…',
```

### 不変条件

- `id` は小文字英数字のみ、重複不可
- **年月は `'YYYY-MM'` の文字列**。数値にしてはいけない。以前は `2025.10` のような
  小数で持っていたが、float にすると `2025.1` に潰れて **10 月が 1 月に化け**、さらに
  `.11`（11月）が `.5`（5月）より小さくなって**年内の並び順が壊れていた**。
  レイアウトは `tnum()` が `年 + (月-1)/12` に変換して使う
- レーン id は `sparse` `linear` `recur` `ssm` `exact` `dist` `kvarch` `kvpost` `sys` `hybrid` `eval` の 11 種
- 親 id が存在しないと辺が静かに落ちる。追加後は必ず検証スクリプトを回す
- 論文名・著者を**確認できなかったら空文字にする**（後述）
- `SUM` の要約は**アブストラクトと本文の内容に基づいて書く**。読まずに機構名から
  推測して書かない。150 字程度を目安とする

## 守るべきこと

1. **確認できないものを埋めない。** 論文名・著者・arXiv ID・数値を推測で書かない。
   一次情報に到達できなければ空文字（`META`）または `要確認` フラグにする。
   `primary_source_url` は arXiv ID がある場合だけ確定し、それ以外は `null` にしてある。
   ブログや GitHub の URL を推測して埋めないこと。
2. **`要確認` を消すのは一次情報で裏が取れたときだけ。** 現在の未確認項目は
   `review.html` 第 10 章「出典と検証状況」に全て列挙してある。
3. **誤りは削除せず訂正として残す。** 本レビューは初版の誤り（Qwen 3.8 の実在、
   および 2026 年の中国勢を「分岐」として立てた構成）を訂正の形で本文に明記している。
   同じ方針を維持すること。
4. **二次情報と一次情報を区別して書く。** 「モデルカードに基づく」「config.json に基づく」
   のように根拠の所在が読者に分かるようにする。

## レビュー論文の骨格

議論の軸は 4 つ。新しい手法を足すときはこの軸のどれを動かすのかを明示する。

| 軸 | 問い |
|---|---|
| A. FLOPs | 漸近計算量を下げたか |
| B. 実体化 | n×n をメモリに置かずに済むか |
| C. 状態サイズ | KV／再帰状態を劣線形にしたか |
| D. 厳密性 | 出力が数学的に厳密か |

主要な主張（変更する場合は根拠を伴うこと）:

- この分野の論争の多くは意見の対立ではなく**問題定義のすれ違い**だった
- FlashAttention は **FLOPs を下げていない**。実体化を消して IO を下界まで削っただけ
- 線形 Attention・線形 RNN・SSM は **SSD 双対性により同一の行列ミキサー**
- 定数状態には**情報理論的な想起容量の上限**と **TC0 という複雑性の天井**の 2 つの壁がある
- 2024–2026 は**収束**であって分岐ではない。厳密 Attention を層の 8〜27% に残す構造で
  東西が一致し、分かれるのは各スロットに何を置くかだけ

## 章構成（`review.html`）

`id="s1"`〜`id="s10"` の 10 章。TOC は `<nav class="toc">` にある。
章を増減したら **TOC・見出しの `id`・`.n` 内の番号・本文中の相互参照**を全て揃えること
（相互参照は「第 N 章」「第 N.M 節」の形で本文に埋まっている）。

## 公開版の更新

公開済み Artifact は**ファイルパスで同一性が判定される**。ローカルのパスから素朴に
publish すると**別の Artifact が新規作成される**。既存 URL を維持するには:

```
1. Artifact action="read"  url=<対象 URL>          現在の公開内容を取得
2. ローカルで編集
3. Artifact file_path=<パス>  url=<対象 URL>        同じ URL を更新
```

URL は `README.md` の成果物表にある。

## 描画まわりの実装メモ（`map.html`）

過去に壊した箇所なので、触る前に読むこと。

- **軸ラベルはズーム対象の外に置いてある。** 年とレーン名は `vp`（拡大縮小されるグループ）
  ではなく `chrome` 層に画面座標で描く。拡大率によらず同じ大きさで読めるようにするためで、
  単にフォントを大きくすると拡大時に文字がノードを覆ってしまう
- **最大ズームアウトは画面形状に追従する。** 年あたりの横幅 `XS` を自由変数として、
  描画全体の縦横比がペインの縦横比に一致するよう反復調整してからレイアウトを確定する。
  軸フレームぶんの余白（左 `GUT` / 上 `HEADER_PX` / 下 `HINT_PX`）は画面 px で決め、
  縮尺が決まってから世界座標へ逆算する。縮尺と余白が相互依存するため数回反復している
- **罫線には `vector-effect:non-scaling-stroke` を当ててある。** これがないと
  最大ズームアウト（倍率 0.16 程度）で 1px の線が 0.16px になり事実上消える
- **ドラッグは動き始めてからポインタを捕捉する。** `pointerdown` の時点で
  `setPointerCapture` すると、続く `pointerup` がキャンバスへ retarget されて
  **ノードとボタンの `click` が発火しなくなる**。5px 動いてから捕捉し、
  パン直後の click は 1 回だけ握り潰している
- **その握り潰しから `.zoom` は除外する。** click ハンドラは捕捉フェーズなので
  ボタンより先に走る。除外しないと、パンやピンチの直後の 1 回だけ
  ズームボタンが効かなくなる
- **ピンチは自前実装が要る。** `touch-action:none` でブラウザ既定のピンチを
  止めているため、実装しないと触覚デバイスで一切ズームできない（iOS で実際に
  効かなかった）。`pts` に生きているポインタを持ち、2 本以上なら 2 指の中点で
  パンしつつ距離比で拡大縮小する
- **`<meta charset>` と `<meta name="viewport">` をファイル自身が持つこと。**
  Artifact の wrapper がこれらを補うため書かずに済んでいたが、GitHub Pages は
  生ファイルを配信するので、無いと iOS が 980px 幅で描画して全体が極小になる
- **狭い画面では軸ラベルを間引く。** 年もレーン名も、直前のラベルと間隔が
  取れないものは落とす。レーンの色帯は残すので構造は見えたままになる

## 検証

```bash
# 整合性検査 + 生成物の更新（失敗時は終了コード 1）
python3 tools/export_papers.py

# HTML は依存パッケージなしの単一ファイル。ブラウザで直接開いて確認する
open map.html
open review.html
```

`map.html` の手動確認項目:

- 最大ズームアウトで縦横とも画面をほぼ埋めること（レターボックスが出ない）
- ノードをクリックして詳細パネルが開き、祖先・子孫がハイライトされること
- ノード上からドラッグしたときにパンだけが起き、選択が切り替わらないこと
- ズームボタン（`+` `−` `⊡`）とホイールが効くこと
- **パンやピンチの直後でも**ズームボタンが効くこと
- 375px 幅でピンチ拡大縮小ができ、軸ラベルが重ならないこと

過去に壊れた箇所なので、描画まわりを触ったら毎回この 6 つを見ること。
ピンチは 2 つの `PointerEvent`（`pointerType:'touch'`、別 `pointerId`）を
合成すればブラウザ上で検証できる。

## 調査の来歴

4 系統を並行して文献調査した（第一波の近似 Attention / IO 認識型厳密 Attention /
KV キャッシュ最適化 / SSM・線形再帰・ハイブリッド）。その後 2026 年の各社アーキテクチャに
ついて追加調査を 2 本行い、2 本目は Qwen3.8-Max とフル Attention 層の下限に関する
アブレーション研究を対象として、初版の誤りの訂正に用いた。

さらに、同一テーマで別途行われた独立調査（51 件の一覧）と arXiv ID を鍵に機械的に
突き合わせた。**51 件中 43 件が既存と重複し、新規増分は 8 件**だった。重複率の高さは、
両調査が主要文献の骨格について独立に同じ結論へ達したことを意味する。追加した 8 件は
いずれも arXiv の abstract ページに対して題名・著者・機構を再確認しており、その過程で
外部一覧の短縮名 2 件が実際の題名と異なることも判明している。

arXiv ID と会議名は可能な限り arxiv.org の abstract ページまたは公式プロシーディングスに
対して照合している。

## 未解決の宿題

- `data/papers.json` の `primary_source_url` が `null` の 10 件（ブログ・GitHub・
  モデルカード由来）は、URL を特定できれば `map.html` の venue 欄と併せて更新できる
- `META` で論文名が空の 1 件（id: `softmaxvslinear`, arXiv:2310.11685）は原題未確認
- `review.html` 第 9 章「未解決問題」に、研究上の open question を列挙してある
